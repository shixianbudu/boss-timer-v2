# 3.0 安全版部署指南（防乱点改造）

这次改造把数据同步从「公共 MQTT（人人可写）」改成了「Cloudflare Worker 后端（唯一权威）」。你需要做的只有一件事：**部署 Worker**（约 10 分钟，免费）。前端合并 PR 后按你原来的方式发布即可。

## 改造后的安全机制

| 机制 | 说明 |
|---|---|
| 服务端权威时间戳 | 击杀时间以服务器时钟为准，改本地时间无效 |
| 刷新周期校验 | 距离上次击杀不足（刷新周期 − 3 分钟）时，拒绝再次记录 |
| 频率限制 | 同一身份 15 次/分钟，同一 IP 60 次/分钟 |
| 自动封禁 | 1 小时内异常操作达 10 次 → 自动封禁该设备 24 小时 |
| 操作动态页 | 所有操作署名公开可见（含被拒绝的），乱来一目了然 |
| 管理员功能 | 输入 ADMIN_KEY 后可：封禁/解封、撤销任意一条操作、全部清空 |
| 全部清空保护 | 仅管理员可执行 |

## 第一步：准备 Cloudflare 账号（免费）

1. 打开 https://dash.cloudflare.com/sign-up 注册（邮箱即可，不用绑卡）
2. 注册后进入 dashboard，右侧能看到你的 **Account ID**，不用记，后面用不到也行

## 第二步：安装 wrangler

如果你电脑上已经有 Node.js（做这个项目应该有），直接打开终端，进入项目目录的 `worker` 文件夹：

```bash
cd worker
npm install -D wrangler   # 或者全局装：npm install -g wrangler
```

## 第三步：登录并创建 KV 存储

```bash
npx wrangler login
```

会弹出浏览器让你授权，点 Allow。

```bash
npx wrangler kv namespace create BOSS_TIMER_KV
```

命令会输出类似：

```
{ binding = "KV", id = "abc123def456..." }
```

把这个 `id` 填进 `worker/wrangler.toml` 里的 `id = "..."` 位置。

## 第四步：设置管理员密钥

```bash
npx wrangler secret put ADMIN_KEY
```

提示输入时，填一个只有你自己知道的密码（比如一段随机字符串），回车。**这个密码就是你网页上「⚙️ 管理」里要输入的密钥。**

## 第五步：部署

```bash
npx wrangler deploy
```

成功后会显示你的 Worker 地址，类似：

```
https://boss-timer-api.xxx.workers.dev
```

可以验证一下，浏览器打开 `https://你的地址/api/health`，看到 `{"ok":true,...}` 就说明活了。

## 第六步：告诉前端 Worker 地址

打开 `src/lib/api.ts`，把最上面的：

```ts
const DEFAULT_API_BASE = 'https://boss-timer-api.YOUR-SUBDOMAIN.workers.dev'
```

改成你的实际地址，然后提交代码、按你原来的流程发布 GitHub Pages。

> 小技巧：不改代码也行——发布后在浏览器控制台执行
> `localStorage.setItem('bt-api-base', 'https://你的地址')` 再刷新，适合先测试。
> 但其他人也要这么设置，所以最终还是要改代码里的默认值。

## 日常使用

- **普通用户**：第一次打开会要求填昵称，之后正常点击即可，操作都会署名显示在「🛡️ 操作动态」页。
- **你是管理员**：点页面右上角「⚙️ 管理」，输入 ADMIN_KEY。之后在「操作动态」里每条记录旁边会出现「撤销」和「封禁」按钮。
- **误封了怎么办**：调用解封接口，或者到 Cloudflare dashboard → KV → 找到 `bans` 这个 key 手动删掉对应条目。

## 常见问题

**Q：免费额度够用吗？**
Workers 免费版每天 10 万次请求，KV 免费版每天 10 万次读。你们几十个人用，绰绰有余。

**Q：懂技术的人能绕过吗？**
可以伪造昵称和随机 ID，但**设备指纹**很难变，封禁是按指纹来的。要彻底换指纹得换浏览器/设备，成本远高于收益。再配合限频和自动封禁，基本无解。

**Q：原来的 MQTT 数据怎么办？**
第一次部署后是全新空表。旧数据是公共 MQTT 里的，污染风险高，建议直接重新开始记录。

**Q：想改限频/封禁阈值？**
都在 `worker/src/index.ts` 顶部的常量区，改完重新 `npx wrangler deploy` 即可。
